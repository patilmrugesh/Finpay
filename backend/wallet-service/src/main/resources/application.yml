server:
  port: 8084

spring:
  application:
    name: wallet-service
  datasource:
    url: jdbc:postgresql://localhost:5435/finpay_wallet_db
    username: finpay_user
    password: "0000"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: false

  gateway:
    internal-secret: finpay-internal-secret-key-2026